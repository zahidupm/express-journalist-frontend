import React from 'react';
import useTitle from '../hooks/useTitle';

const AllReview = () => {
    useTitle('All Review')
    return (
        <div>
            <h2>all review</h2>
        </div>
    );
};

export default AllReview;